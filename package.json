{
  "name": "countdown-forest",
  "private": true,
  "version": "1.3.0",
  "type": "module",
  "scripts": {
    "audio:build": "node scripts/audio-pipeline.mjs",
    "visual:validate": "node scripts/validate-visual-assets.mjs",
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "@types/node": "^22.20.1",
    "jsdom": "^29.1.1",
    "typescript": "^5.8.3",
    "vite": "^6.3.5",
    "vitest": "^3.1.4"
  }
}
